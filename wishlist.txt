List of desired further improvements.

* Implement back-throttling of transmissions. This means that if link of receiving node is full, the sending node will reduce sending speed accordingly. It might also notify testing node about data sending speed.
